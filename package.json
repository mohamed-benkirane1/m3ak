{
  "name": "m3ak",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run build --workspace @m3ak/shared && npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspace @m3ak/shared && npm run build --workspaces --if-present",
    "test": "npm run build --workspace @m3ak/shared && npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
